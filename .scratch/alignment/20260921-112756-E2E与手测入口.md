# 对齐成果 — youtubesub 真浏览器 E2E 与手动验收入口

- 日期：2026-09-21
- 来源：单次会话（ask-matt 路由 → /implement 段，推进到手动验收）
- 人称读法：我 = agent（提问/判定），你 = 用户（回答/裁决）；每条均已标注归属

> **落盘说明（落盘时补记，非原文）**：本文按用户粘贴的原文逐条落盘，未压缩、未合并、未改写措辞。
> 文内相对链接写于"仓库根目录"语境（如 README.md），本文件位于 .scratch/alignment/，点击时需自行加 `../../` 前缀。
> 状态：D/K/C 是已发生事项的记录；**O 类待决与 A/B 类的"请裁决"项尚未裁决**，等用户逐条回答后再更新本文。

---

## 一、目的地（D）

- **D1 本轮目的地** · 出处：你开场 →「自检目前的项目情况，直到推进到让我手动检验成果的阶段」（会话第一句，走 `/ask-matt` 路由）。我的执行解读：先自检文档声称的状态，再把项目推到"你能亲手验收的入口齐备"为止；到点即停，不替你验收。
- **D2 项目终极目的地** · 出处：文档既有+你沿用 → [README.md](README.md)「目标: 浏览器播放 YouTube, 网页最小化/被遮挡时仍能听声, 独立 always-on-top 桌面浮窗显示与播放进度同步的字幕」；三模式（原文/译文/双语）；翻译走 OpenAI-compatible API（Key/BaseURL/Model/Prompt 可配）。
- **D3 本轮流程定位** · 出处：我判定（ask-matt 路由）→ 处于主流程 `/implement` 段，下一步是 [PROGRESS.md](PROGRESS.md) §3.0(A)；内部驱动 `/tdd`（红→绿），收尾做双轴评审。状态：已据此执行，你未反对。
- **D4 "手动检验"分四层** · 出处：我判定待你裁决（由 D1 推出）→ ①夹具演示（零安装零 Key）②真实站点诊断（零安装）③真 Tampermonkey 全场景 ④真实 AI Key 翻译链路。①② 我做出了入口，③④ 需要你。
- **D5 真实 YouTube 的归属** · 出处：你原话 →「real youtube归纳到用户手动验收」（上下文：我正在真站点上追根因、已有 tracer 证据）→ 停止自动化攻它，只留 `--live` 诊断入口并把结论如实记录。

## 二、约束与偏好（K）

- **K1 技术栈** · 出处：文档既有+你沿用 → [CONTEXT.md](CONTEXT.md) 访谈结论「桌面栈: Python + PySide6」「浏览器端: Tampermonkey 用户脚本」。
- **K2 硬约束（不做）** · 出处：文档既有+你沿用 → [README.md](README.md)「不做屏幕 OCR; 不做 Whisper ASR fallback; 不一条 cue 一个请求」「桌面端不直接抓 timedtext (pot 只能在页面内复用)」。
- **K3 凭证时序** · 出处：文档既有+你沿用 → CONTEXT「AI 凭证: 用户稍后提供真实 Key/BaseURL/Model; 在此之前用 Mock 翻译服务验证链路, AI 成功场景记为部分验证」。
- **K4 许可证边界** · 出处：文档既有+你沿用 → CONTEXT「dkitle: Rust 端无 LICENSE…只参考设计, 不抄代码」；yt-dual-subs/transly（MIT）可改写复用保留版权；LiveSubs/local-screen-translator（Apache-2.0）可复用保留声明。详见 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。
- **K5 密钥与隐私** · 出处：文档既有+你沿用 → README「Key 只存本机 `%APPDATA%/SubOverlay/setting.json`, 不进 Git/日志/页面」；CONTEXT「`/status` 会回显字幕文本（仅 loopback, 无 provider 时不回显）」。另见 [ADR-003-loopback-security](docs/adr/ADR-003-localhost-security.md)。
- **K6 本机环境约束** · 出处：文档既有+你沿用 → CONTEXT「DNS 污染, 直连 YouTube 失败; 代理 `127.0.0.1:10809` 可用」；loopback 不走代理；PowerShell 执行策略拦 `.ps1`；**不是 git 仓库（无版本控制兜底）**。
- **K7 不许动用户自己的东西** · 出处：文档既有+你沿用 → PROGRESS「本机浏览器是用户自己在用的…不要 kill 它们; E2E 必须用独立 `--user-data-dir` + 独立调试端口」。本轮遵守方式：独立临时 profile + 空闲端口 + 临时 APPDATA；收尾已确认无残留进程。
- **K8 浮窗产品偏好（你上一轮的反馈）** · 出处：文档既有+你沿用 → PROGRESS §2.5「去掉窗口最小尺寸限制」「字号下限 8->6, 默认 15->10」「修掉 resize 严重 bug: 放大后往内拖无法缩小」。本轮未动这些行为。
- **K9 本轮的元约束（工作方式）** · 出处：你原话 →「告一段落。暂停推进，执行「思维对齐 · 成果沉淀」…」（五类逐条、不压缩不合并、标出处、单列可疑遗漏、列完停下等你逐条核对、确认后才落盘、正文先写文件再写回单据、换行不许写成 `\n`）。我的遵守状态：**未写任何文件**。

## 三、已确认的决定（C）

- **C1 流程决定** · 出处：我执行+你未反对 → 按 `/implement` 主线推进，内部 `/tdd` 红→绿，收尾做双轴（标准/规格）评审。
- **C2 P0 落地为真实浏览器 E2E** · 出处：我执行+证据（方案源自 PROGRESS §3.0，上一轮已锁定）→ 新建 [browser_e2e.py](desktop/tests/browser_e2e.py)（夹具页 + 真 WAV + 真 Chrome + CDP document-start 注入**真 userscript** + 真 `app.py` 子进程）+ [test_browser_e2e.py](desktop/tests/test_browser_e2e.py) 7 条。证据：65 passed；无 Chrome 则 skip 而非 fail。
- **C3 观察口只用 `GET /status`（黑盒）** · 出处：我执行+证据（沿 PROGRESS §3.0 步骤 7）→ 7 条 E2E 全部经 `/status` 断言，不碰内部 API。
- **C4 夹具的三个硬性细节** · 出处：我执行+证据（① 上一轮勘定，②③ 本轮实测补正）→ ①页面 URL 必须 `http://127.0.0.1:<port>/watch?v=…`（否则 WS Origin 被 403）②timedtext URL 路径含字面量 `youtube` 才过 `isTimedtextUrl` ③`tOffsetMs` 必须是"段内小偏移"而非绝对时间戳（我第一版写错，导致分句被合并、演示显示拼接串，已修）。证据：E2E + demo 实跑。
- **C5 真 bug#1 与修法：cues 到达时清零播放时钟** · 出处：E2E 首次红暴露 → 我定位到 [engine.py](desktop/suboverlay/engine.py) `handle_event` 的 `src.sync = SyncState()`；决定"同 source 保留活时钟，新 source 仍从零开始"。证据：`test_cues_refresh_keeps_a_live_clock` 先红后绿 + SPA 用例转绿。
- **C6 真 bug#2 与修法：click-through 无解锁路径** · 出处：我读 [overlay.py](desktop/suboverlay/overlay.py) + [app.py](desktop/app.py) 发现（PROGRESS 曾声称"click-through+解锁"，实际开启后鼠标事件被忽略、右键菜单够不着，只能杀进程）→ 决定实现 Ctrl+Alt+U（`GetAsyncKeyState` 轮询 + 上升沿只触发一次，reader 可注入）。证据：新增 [hotkey.py](desktop/suboverlay/hotkey.py) + [test_hotkey.py](desktop/tests/test_hotkey.py) 4 条（含 App 级解锁用例）。
- **C7 真 bug#3 与修法：菜单构造时序** · 出处：我写的 App 级测试红暴露 → `_ct_action` 只在 `run()` 里创建，而热键定时器可能先跑 → 决定把 `_ctx_menu = self._menu()` 移进 `__init__`。证据：该测试转绿。
- **C8 真 bug#4（本轮最重要）与修法：Trusted Types 让 page hook 静默失败** · 出处：`--live` 输出的浏览器 Log →「This document requires 'TrustedScript' assignment」+「[youtubesub] page hook injection failed」，而面板仍显示 `connected` → 决定 ①三级回退：`GM_addElement` → TrustedScript policy → 主世界 `new Function`（在 userscript 沙箱内**跳过**，免得把钩子装进错的 realm）②把失败沿 `register.hook_error` → engine meta → `/status` → 浮窗状态行 + 面板 `[NO PAGE HOOK]` 暴露。证据：node 29→33、pytest 62→65（均先红后绿）。
- **C9 两个人入口的形态** · 出处：我执行+证据 → `--demo`（headed + 夹具 + 可见浮窗 + 自动播放，零安装零 Key）与 `--live URL`（真实站点，独立 tracer 把链路拆成"站点是否发出请求 / 钩子是否抓到 / app 是否收到"三段）。证据：两者均实跑；demo 逐条字幕、暂停冻结、末 cue 后清空；live 打出三段诊断。
- **C10 真实站点模式的两处"测试特权"** · 出处：我判定待你裁决 → Chrome `--disable-web-security`（顶替 GM_xmlhttpRequest 的 CORS 旁路；PROGRESS §3.0 上一轮已定）+ `Page.setBypassCSP`（顶替 GM_addElement 的 CSP/TT 特权；本轮新增，为区分"harness 原因失败"与"产品原因失败"）。
- **C11 结论收敛（在你第三句之后）** · 出处：你原话+我执行 → 真实 YouTube 不再自动化，归手动验收；只保留 `--live` 入口并把结论如实记录。
- **C12 诚实记录：原计划的验收配套尚未落地** · 出处：我的计划（被你的暂停指令中断）+ 根目录核对证据 → `requirements.txt`、`start-desktop.cmd`、`docs/MANUAL-ACCEPTANCE.md`、README 运行章节更新、PROGRESS/CONTEXT 更新，**一个都没写**。

## 四、待决问题（O）

- **O1** 真实 Base URL/Key/Model 何时提供？（CONTEXT 记载"稍后"；在此之前第 ④ 层只能记部分验证）
- **O2** 是否要装 Tampermonkey（需你手动）以跑真扩展全场景？出处：CONTEXT「Chrome User Data 里没有 Tampermonkey…需要用户手动装」。
- **O3** 真实 YouTube 抓不到 cue 的根因**未定论**。最新一次 live 实跑现状：`hookError` 已为空（注入成功）、`timedtextSeenByTracer: 1`（站点确实在发 `…/api/timedtext?…&fmt=json3&pot=…`）、但 `bridgeTrackKey` 空、`/status` 一直 `no_cues`。我的首要假设：注入的包装器在 DOMContentLoaded 之后被 YouTube 自己替换/恢复了 `window.fetch`，钩子被摘掉。一行可验证的下一步：读 `window.fetch.toString()` 是否仍含 `youtubesub-timedtext` 标记。归：手动验收（按你指令不推进）。
- **O4** `--live` 里的 `Page.setBypassCSP` 你是否接受（它让 live 更接近 Tampermonkey，但确实关掉了 CSP）？是否要在验收文档里明示？
- **O5** 验收配套（依赖清单 / 启动脚本 / 验收清单 / README）**现在补**，还是先让你验收再补？
- **O6** 落盘位置：已核对"无 ticket 也无 map"，按模板应落 `.scratch/alignment/<ts>-<短标题>.md`（需你同意新建 `.scratch/`）。短标题建议 `youtubesub-手测入口` 或 `e2e-与信任类型`，请指认。
- **O7** P2 清单哪些进本轮：历史字幕行渲染 / README 运行章节 / 打包分发 / 多标签 source UI（click-through 文案与热键本轮已随之解决）。
- **O8** 收尾评审：主流程要求 `/implement` 末尾做 `/code-review`，但项目**不是 git 仓库、没有 fixed point**，只能改为"对本次改动清单做双轴评审"。是否接受这种替代？
- **O9** 是否要把本轮成果同步进 PROGRESS.md / CONTEXT.md？它们现在仍写着"浏览器侧结论仍不可信"，与本轮已完成的 E2E 事实**不符**。我判断这属于"落盘"的一部分，需你确认。

## 五、雾区（F）

- **F1** "让我手动检验成果"的验收边界与判据：四层里要检到哪一层算过？我按"四层都给入口"做，但**未与你确认优先级与判据**。
- **F2** 真 Tampermonkey 能否过 Trusted Types（`GM_addElement` 的扩展特权是否真的绕开）——我没有该环境，只有推测；这直接决定第 ③ 层能否通过。
- **F3** 若真 Tampermonkey 也抓不到，是否转向别的抓取点（例如内嵌 player response 的 `captionTracks[].baseUrl`，或 player `getOption('captions')` 拿到的 URL）——架构级分叉，我未探。
- **F4** 是否要从单文件 PROGRESS.md 迁移到 ticket/map 工作流（本次沉淀指令暗示该体系存在）——若迁移，谁建图、以什么粒度切票，我不明。
- **F5** 本次"思维对齐"的粒度与节奏（是否每次告一段落都做、是否跨会话累积）——我只见到这一次。
- **F6** 我对模板人称的读法是否正确（见文件头"人称读法"）——若你要"用户提问→agent 回答"方向，请指出。

## 可疑遗漏

### A 类：你问过、我已答、但没进上面清单

- **A1 自检的核对结论本身**：50/29 与文档一致（现已 65/33）、不是 git 仓库、9877 端口未被占、无残留进程。· 我的判断理由：属"自检证据"而非"对齐条目"，故只在开头作为证据引用。· **请裁决：纳入 or 放弃。**
- **A2 E2E 到底证明了什么**（a 页面钩子在真 Chrome 主世界抓到真请求 / b 真 Origin 过白名单 / c 真 cue 文本进 Engine 与浮窗 / d play-pause-seek-rate-SPA 语义）。· 理由：已折进 C2/C3，没单列为"目的地达成度"。· 请裁决。
- **A3 `--demo` 的实测观感**（逐条字幕严格按 cue 切换、暂停冻结、末 cue 之后清空不 hold）。· 理由：属验收证据，归到 C9 与手动验收。· 请裁决。
- **A4 你第二句只是「continue」**（续跑中断的调用）。· 理由：我未把它当成对齐条目。· 请裁决是否记为"你要求继续"。

### B 类：我提过、但判断无关主线/太模糊/属执行细节而未纳入

- **B1 控制台把 `【译】` 显示成乱码**（cp936）。· 理由：纯显示层，浮窗才是真证据，且我已给 demo 加 UTF-8 reconfigure。· 若你要"验收时控制台也好看"，纳入。
- **B2 demo 实跑中 `playing` 出现过一次 False→True 抖动**。· 理由：我判断是外部点击或媒体初始化，非产品语义（E2E 的 play/pause 断言通过）。· 是否立一条"待观察"？
- **B3 `data/translations.db` 会被 mock 结果写入**（夹具 video_id 维度）。· 理由：无害且不含 Key。· 请裁决。
- **B4 [fake_browser.py](desktop/tests/fake_browser.py)（上一轮的 GUI 驱动脚本）仍保留**，与新 E2E 职责重叠。· 理由：它无需 Chrome 仍可用。· 保留 / 标记 / 删？
- **B5 [test_browser_e2e.py](desktop/tests/test_browser_e2e.py) 有用例顺序依赖**（SPA 用例必须最后，因为它换视频换 source）。· 理由：已在文件头注明，属维护注意。· 是否要改成独立 fixture？
- **B6 探测脚本（`%TEMP%\ytus-probe\*.py`）未入仓库**。· 理由：一次性诊断，结论已折入 O3。· 是否要留档进 `docs/`？
- **B7 夹具的 HTTP Range 支持**（不支持 Range 会让 seek 被钳到 ~0.5s，E2E 的 seek 断言会**假红**）。· 理由：已折进 C4，但这条重要性高，我漏单列。· 请裁决是否补列为独立决定。
- **B8 Chrome 153 + `--headless=new` 可用、CDP `/json/list` 里会混入扩展的 `background_page` target（必须挑 `type=page`）**。· 理由：纯执行细节，已写进 harness 代码。· 是否进"环境事实"清单？
- **B9 `register` 帧新增可选字段 `hook_error`，但 [docs/PROTOCOL.md](docs/PROTOCOL.md) 未更新**。· 理由：我判断这是**应当补的遗漏**（向后兼容的可选字段，`sanitize_event` 不校验额外键，但协议文档是规范来源）。· **我倾向纳入本轮**，请裁决。
